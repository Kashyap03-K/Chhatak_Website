# Root-level env for docker-compose.prod.yml (Postgres credentials only).
# Copy to .env on the VPS and fill with strong values.
POSTGRES_USER=chhatak
POSTGRES_PASSWORD=REPLACE_WITH_STRONG_PASSWORD
POSTGRES_DB=chhatak
